@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor httpContentAccessor
@{
    var session = httpContentAccessor.HttpContext.Session;
}
@model IEnumerable<EvergreenAPI.Models.Image>

@{
    ViewData["Title"] = "Image Management";
}


@*
<div class="container">
    <div class="row">

        <div class="col-md-12 row d-flex flex-column align-items-center justify-content-between">
            <div class="col-md-8 row d-flex flex-column align-items-center justify-content-between">
                <input type="text" class="mb-2 py-0 mr-sm-2 col-md-6" id="search" placeholder="Image description or ID">
                <button class="btn btn-primary mb-2 py-0 col-md-6">Seach</button>
            </div>
            <button class="btn btn-primary mb-2 py-0 col-md-2">Upload new image</button>
        </div>

        @foreach (var item in Model)
        {
            <div class="col-md-6 ftco-animate">
                <div class="card flex-md-row mb-4 box-shadow h-md-250">
                    <div class="card-body d-flex flex-column align-items-start">
                        <h4 class="mb-0">
                            <a class="text-dark" href="#">@item.ImageId</a>
                        </h4>
                        <p class="card-text mb-auto text-ellipsis" style="max-width: 248px;">@item.AltText</p>
                        <div class="flex-column">
                            <a class="btn btn-warning rounded" asp-action="Update" asp-route-id="@item.ImageId"> Update </a>
                            <a class="btn btn-danger rounded" asp-action="Delete" asp-route-id="@item.ImageId"> Delete </a>
                        </div>
                    </div>
                    <div class="img-hover-zoom flex-auto d-none d-md-block card-img-right" style="width: 250px; height: 250px; border-top-right-radius: 0.25rem; border-bottom-right-radius: 0.25rem;">
                        <img alt="Thumbnail" style="width: 100%; height: 250px; object-fit: cover;" src="@item.Url" data-holder-rendered="true">
                    </div>
                </div>
            </div>
        }
    </div>
</div>*@
